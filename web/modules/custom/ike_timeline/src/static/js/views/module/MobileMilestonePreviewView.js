define(["jquery", "underscore", "backbone"], function($, _, Backbone) {
  var MobileMilestonePreviewView;
  return MobileMilestonePreviewView = AbstractItemView.extend({
    initialize: function(data) {}
  });
});
